What is a shell?
A shell is a interpreter  that communicate with the kernal
What is Bash?
a bash is a bourn again shell
What does $SHELL show?
it show which shell are we using in my case it is deafault /bin/Bash
What does $0 normally show?
it show login shell in my case _bash
What is the difference between a terminal and a shell?
a terminal is a medium to communicate with a shell

User: it is a person who gives the command
shell: shell is the interpreter that interact with kernal
Kernal: kernel is the core of Linux that manages system resources and communicates with the hardware.
hardware: hardware is the physical compponent such as hardisk,ram,cpu
When a user types a command, which layer reads the command first?
after user give the command  shell is the first component that gives the command

What does mkdir do?
mkdir is use to create directory
What does cd do?
cd is use to change the directory
What does touch do?
touch is used to make file
What does cp do?
cp is used to copy 
What does mv do?
mv is used to move
What does ls -l show?
it display the file and directories in long listing format
What does cat do?
cat is used to rad the data of the file
What does pwd show?
pwd show the current working directory


What does chmod u+x hello.sh do?
it is used to give the user to permission to execute the file hello.sh
Why can bash hello.sh run a readable script without its execute bit?
Because Bash reads the script file directly it only needs read permission not execute permission.
Why does ./hello.sh require execute permission?
Because ./hello.sh runs the script as a program so it needs execute permission
What does ./ mean?
./ means the current directory it tells Linux to run the file from the folder we are currently in



